export const metadata = {
  title: "Import commandes — Spacefill",
  description: "Convertisseur de fichiers CSV/XLS en commandes Spacefill",
};

export default function ImportLayout({ children }) {
  return <>{children}</>;
}
